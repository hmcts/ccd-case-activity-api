nodejs:
  ingressHost: ${SERVICE_FQDN}
  image: ${IMAGE_NAME}
  environment:
    REDIS_HOST: "ccd-case-activity-api-{{ .Values.global.environment }}-redis-master.service.core-compute-{{ .Values.global.environment }}.internal
    REDIS_PORT: 6379
    REDIS_PASSWORD: testPassword
    REDIS_SSL_ENABLED: false
  keyVaults:

redis:
  enabled: true
  ssl: false
  password: testPassword
  ingressHost: ccd-case-activity-api-{{ .Values.global.environment }}-redis-master.service.core-compute-{{ .Values.global.environment }}.internal
