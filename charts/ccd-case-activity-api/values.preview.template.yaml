nodejs:
  ingressHost: ${SERVICE_FQDN}
  image: ${IMAGE_NAME}
  environment:
    REDIS_HOST: "{{ .Release.Name }}-redis-master.service.core-compute-preview.internal"
    REDIS_PORT: 6379
    REDIS_PASSWORD: testPassword
    REDIS_SSL_ENABLED: false
  keyVaults:

redis:
  enabled: true
  ssl: false
  password: testPassword
  ingressHost: "{{ .Release.Name }}-redis-master.service.core-compute-preview.internal"
