nodejs:
  ingressHost: ${SERVICE_FQDN}
  image: ${IMAGE_NAME}
  environment:
    REDIS_HOST: "{{ .Release.Name }}-redis-master"
    REDIS_PORT: 6380
    REDIS_PASSWORD: testPassword
    REDIS_SSL_ENABLED: false
  keyVaults:

redis:
  enabled: true
  ssl: false
  password: testPassword
  ingressHost: "{{ .Release.Name }}-redis-master"

